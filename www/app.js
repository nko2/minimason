var canvas, gl, context;

window.addEventListener('load', function (evt) {

  document.body.textContent = "";
  canvas = document.createElement('canvas');
  document.body.appendChild(canvas);

}, true);
